import { Header2 } from './Header';
import { HeaderAction, headerReducer } from './reducers';

//export type RootActions = HeaderAction[keyof HeaderAction];
export { Header2, HeaderAction, headerReducer };
